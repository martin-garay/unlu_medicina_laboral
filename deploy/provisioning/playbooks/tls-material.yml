---
- name: Install TLS certificate material before Apache configuration
  hosts: app_servers
  gather_facts: true
  become: true
  vars_files:
    - ../group_vars/all.yml
  roles:
    - role: tls
      when: tls_enabled | default(false)
