---
- name: Apply host hardening before firewall
  hosts: all
  gather_facts: true
  become: true
  vars_files:
    - ../group_vars/all.yml
  roles:
    - role: hardening
    - role: firewall
      when: firewall_enabled | bool
