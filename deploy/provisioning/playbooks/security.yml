---
- name: Apply host hardening before firewall
  hosts: all
  gather_facts: true
  become: true
  vars_files:
    - ../group_vars/all.yml
  roles:
    - role: hardening
      when: security_enabled | bool
    - role: firewall
      when:
        - security_enabled | bool
        - firewall_enabled | bool
