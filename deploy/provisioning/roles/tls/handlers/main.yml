---
- name: Update CA certificates
  ansible.builtin.command: update-ca-certificates
  changed_when: true

- name: Reload Apache after TLS change
  ansible.builtin.service:
    name: apache2
    state: reloaded
  when: ansible_facts.services['apache2.service'] is defined
