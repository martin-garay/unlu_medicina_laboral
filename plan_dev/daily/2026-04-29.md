# Daily Plan
## Fecha
2026-04-29

## Objetivo del dia
Detallar la planificación de módulos administrativos/read-only del backoffice antes de implementar Resources de Filament.

El foco es dejar especificadas pantallas, columnas, filtros, permisos, relaciones, restricciones de datos sensibles y tests esperados para poder abrir dailies de implementación concretas sin volver a definir alcance.

---

## Estado actual resumido

- `I1 - Base tecnica de Filament`: `done`.
- `I2 - Auth, usuarios, roles y permisos`: `base_done`.
- `I3 - Auditoria administrativa base`: `base_done`.
- Panel Filament disponible en `/admin`.
- Acceso al panel restringido por permiso `backoffice.access`.
- Roles base: `admin`, `auditor`, `director`.
- No existen Resources funcionales en `app/Filament/Resources`.
- `I4 - Storage privado de certificados` sigue pendiente y no se implementa en este daily.
- No se debe descargar, visualizar ni exponer archivos medicos.

---

## Reglas de ejecucion de este daily

- Ejecutar milestones en orden.
- Este daily prioriza planificación fina; desde `P2` habilita implementación incremental de Resources read-only ya especificados.
- No implementar `I4`.
- No crear descarga, preview ni links de archivos médicos.
- No exponer `certificado_base64`, `payload_crudo` completo, metadata completa ni `storage_path` como link.
- Todo cambio documental debe validarse con `git diff --check`.
- Todo milestone cerrado con cambios debe quedar en commit chico.

## Decisiones operativas del backoffice local/desarrollo

- En entorno local/desarrollo, el rol `admin` debe tener acceso a todos los permisos definidos en `config/backoffice.php`.
- El seeder `BackofficeRolesAndPermissionsSeeder` debe seguir sincronizando el rol `admin` contra la lista completa de permisos disponibles.
- El usuario local de prueba debe quedar asociado al rol `admin` cuando `BACKOFFICE_LOCAL_ADMIN_ENABLED=true`.
- Acceso total por permisos no significa saltear restricciones funcionales del módulo.
- Si un Resource fue definido como read-only, `admin` puede acceder a la interfaz habilitada, pero no debe crear, editar, borrar ni ejecutar acciones que el módulo todavía no implementa.
- Las restricciones read-only se mantienen especialmente para módulos con datos sensibles o flujos aún no especificados.

---

## P0
### ID
P0

### Nombre
Abrir daily de especificación de módulos administrativos

### Objetivo
Crear el plan diario activo para ordenar el relevamiento y la planificación fina de opciones administrativas.

### Alcance
- crear `plan_dev/daily/2026-04-29.md`
- registrar estado actual
- dejar `P1` como primer milestone de documentación ejecutable
- actualizar `plan_dev/STATUS.md`

### No incluye
- Resources de Filament
- tests runtime
- storage privado

### Validacion automatica obligatoria
- `git diff --check`

### Validacion manual sugerida
- confirmar que el daily no salta a implementación ni a `I4`

### Estado
`done`

### Resultado de ejecucion
- Daily creado para planificar módulos administrativos read-only.
- `P1` queda cubierto en este mismo corte documental.

---

## P1
### ID
P1

### Nombre
Especificar módulos administrativos read-only y estrategia de tests

### Objetivo
Crear documentación detallada para implementar módulos del backoffice con tests, empezando por opciones read-only.

### Alcance
- crear `docs/backoffice/module-specs.md`
- relevar modelos administrativos actuales
- definir módulos:
  - conversaciones read-only
  - avisos read-only
  - anticipos/certificados read-only
  - auditoría read-only
  - dashboard mínimo
  - informes/exportaciones como planificación futura
- definir columnas, filtros, búsqueda, detalle, relaciones y acciones permitidas/prohibidas
- definir tests esperados por módulo
- documentar criterio de uso futuro de Dusk/Playwright
- actualizar enlaces en documentación de backoffice
- actualizar `plan_dev/STATUS.md`

### No incluye
- implementar `ConversacionResource`
- implementar `AvisoResource`
- implementar `AnticipoCertificadoResource`
- implementar `AuditoriaAdministrativaResource`
- crear Dusk o Playwright
- implementar `I4`

### Dependencias
- `P0` done

### Validacion automatica obligatoria
- `git diff --check`

### Validacion manual sugerida
- revisar si el primer módulo recomendado debe ser `ConversacionResource` read-only
- confirmar si se mantiene la matriz actual `*.view` o si se agregan permisos `*.viewAny`

### Stop conditions especificas
- si se detecta que implementar un módulo requiere storage privado, no implementarlo y dejarlo documentado.
- si faltan criterios de seguridad para payloads o archivos, documentar pendiente y no avanzar a Resource.

### Estado
`done`

### Resultado de ejecucion
- Se creo `docs/backoffice/module-specs.md` con especificación fina de módulos y tests esperados.
- Se dejo recomendado iniciar por `ConversacionResource` read-only.
- `I4` queda expresamente fuera de alcance.

### Validacion ejecutada
- `git diff --check`

### Commit sugerido
- `docs: especificar modulos read-only de backoffice`

---

## P2
### ID
P2

### Nombre
Implementar ConversacionResource read-only

### Objetivo
Implementar el primer módulo administrativo recomendado con tests.

### Alcance
- crear `ConversacionResource`
- listado read-only
- permisos por `conversaciones.view`
- bloquear create/edit/delete
- tests de acceso y listado

### No incluye
- responder mensajes
- reprocesar conversaciones
- editar estado
- mostrar payload completo
- crear E2E con Dusk/Playwright

### Dependencias
- `P1` done

### Validacion automatica obligatoria
- `make test`
- `git diff --check`

### Estado
`done`

### Resultado de ejecucion
- Se creo `ConversacionResource` read-only bajo el grupo de navegación `Trazabilidad`.
- Se agrego listado de conversaciones con columnas operativas y búsqueda por `wa_number`, `dni` y `uuid`.
- Se restringio el acceso al permiso `conversaciones.view`.
- Se bloquearon create/edit/delete y acciones masivas.
- Se agregaron tests de acceso por permisos/roles, listado Livewire/Filament y modo read-only.

### Validacion ejecutada
- `make test`: `148 passed`, `492 assertions`.
- `git diff --check`: sin errores.

### Commit sugerido
- `feat: agregar resource read-only de conversaciones`

---

## P2.1
### ID
P2.1

### Nombre
Documentar acceso total de admin en desarrollo

### Objetivo
Dejar explícito que el usuario/rol `admin` en desarrollo tiene todos los permisos disponibles, sin romper restricciones read-only definidas por módulo.

### Alcance
- documentar la regla en este daily
- documentar la regla en `README.md`
- aclarar la diferencia entre permisos totales y acciones funcionalmente bloqueadas

### No incluye
- implementar `UserResource`
- implementar `RoleResource`
- agregar bypass de autorización
- habilitar create/edit/delete en módulos read-only

### Dependencias
- `P2` done

### Validacion automatica obligatoria
- `git diff --check`

### Estado
`done`

### Resultado de ejecucion
- Se agrego una decisión operativa para backoffice local/desarrollo.
- Se documenta que `admin` sincroniza todos los permisos desde `config/backoffice.php`.
- Se documenta que las restricciones read-only siguen vigentes aunque el usuario tenga permisos totales.

### Commit sugerido
- `docs: documentar acceso admin total en desarrollo`

---

## P3
### ID
P3

### Nombre
Agregar visualización de historial de conversación

### Objetivo
Agregar una acción visual de `View` con icono de ojo en `ConversacionResource` para abrir una pantalla read-only que muestre todo el hilo de interacción entre chatbot y usuario.

### Alcance
- agregar permiso `conversaciones.historial.view`
- sincronizar el permiso en `config/backoffice.php` para roles autorizados
- mantener `conversaciones.view` como permiso de acceso al listado
- agregar acción visual de ojo en la tabla de conversaciones
- crear pantalla de visualización read-only para una conversación
- mostrar resumen de conversación
- mostrar timeline/hilo de mensajes entrantes y salientes
- diferenciar visualmente usuario/chatbot usando `ConversacionMensaje.direccion`
- mostrar campos seguros del mensaje:
  - `direccion`
  - `tipo_mensaje`
  - `step_key`
  - `contenido_texto`
  - `es_valido`
  - `motivo_invalidez`
  - `message_key`
  - `template_name`
  - `created_at`
- mostrar eventos de trazabilidad asociados:
  - `tipo_evento`
  - `step_key`
  - `descripcion`
  - `codigo`
  - `created_at`
- ordenar mensajes y eventos cronológicamente
- agregar tests de permisos, acción visible/oculta, detalle e hilo

### No incluye
- responder mensajes
- reenviar mensajes
- reprocesar conversación
- modificar estado de conversación
- editar mensajes o eventos
- mostrar `payload_crudo` completo por defecto
- mostrar metadata completa por defecto
- agregar Dusk o Playwright

### Reglas de permisos
- usuario con `conversaciones.view` puede acceder al listado.
- usuario con `conversaciones.historial.view` puede abrir la pantalla de historial.
- si un usuario puede listar pero no tiene `conversaciones.historial.view`, no debe ver la acción de ojo y no debe poder entrar por URL directa.
- en desarrollo, `admin` debe recibir este permiso al ejecutar el seeder, por la regla de admin total documentada.

### Dependencias
- `P2` done

### Validacion automatica obligatoria
- `make test`
- `git diff --check`

### Tests esperados
- usuario sin `backoffice.access` no accede.
- usuario con `backoffice.access` pero sin `conversaciones.view` no accede al listado.
- usuario con `conversaciones.view` pero sin `conversaciones.historial.view` ve listado pero no ve acción de ojo.
- usuario con `conversaciones.historial.view` ve acción de ojo.
- usuario con `conversaciones.historial.view` puede abrir detalle/historial.
- acceso directo por URL al historial queda denegado sin permiso.
- mensajes entrantes y salientes se muestran ordenados.
- eventos se muestran ordenados.
- `payload_crudo` no aparece completo por defecto.
- metadata completa no aparece por defecto.
- no aparecen acciones de responder, reenviar, reprocesar, editar o borrar.

### Estado
`done`

### Resultado de ejecucion
- Se agrego el permiso `conversaciones.historial.view` a la matriz de backoffice.
- Se sincronizo el permiso para `admin`, `auditor` y `director`.
- Se agrego acción visual `Ver historial` con icono de ojo en el listado de conversaciones.
- Se creo la pantalla read-only de historial de conversación.
- Se muestra resumen de conversación, hilo de mensajes usuario/chatbot y eventos de trazabilidad.
- Se evita exponer `payload_crudo` y metadata completa; también se limpia `Conversacion.metadata` del estado Livewire de la página.
- Se agregaron tests de permisos, acción visible/oculta, acceso directo, orden de mensajes/eventos y no exposición de campos sensibles.

### Validacion ejecutada
- `make test`: `152 passed`, `526 assertions`.
- `git diff --check`: sin errores.

### Commit sugerido
- `feat: agregar historial read-only de conversaciones`

---

## Criterio de cierre

Este daily queda listo para implementación cuando:

- existe especificación fina de módulos
- el primer módulo recomendado queda claro
- los tests esperados están definidos
- `I4` queda fuera de alcance
- `STATUS.md` refleja el próximo paso
