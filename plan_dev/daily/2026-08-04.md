# Daily 2026-08-04 — Planificación de despliegue Ansible

## Objetivo del día

Relevar el estado real del repositorio y consolidar bajo `deploy/` una planificación completa, revisable y no ejecutable todavía para desplegar con Ansible sobre Debian, manteniendo compatibilidad prevista con Ubuntu 24.04.

## Regla de alcance

- No modificar Laravel, Docker Compose ni el entorno local.
- No implementar roles, playbooks, inventarios funcionales ni secretos.
- Toda documentación y futuro código específico de despliegue vive bajo `deploy/`.
- `plan_dev/` conserva solamente seguimiento operativo y referencias al plan canónico.

## M1 — Relevamiento y plan técnico de Ansible

### Estado

`done`

### Alcance ejecutado

- relevamiento de Docker, variables, PHP, PostgreSQL, scheduler, storage, logs, backups, webhook, operación, scripts, Makefile, CI/CD y antecedentes Ansible/Vagrant;
- definición de topologías con app/DB separadas o compartidas;
- recomendación de Apache + PHP-FPM directo en host;
- estructura futura bajo `deploy/`;
- plan de variables/Vault, PostgreSQL, TLS, releases, scheduler, backups, seguridad, diagnóstico, rollback, pruebas y Vagrant;
- etapas pequeñas y tabla de decisiones pendientes.

### Archivos previstos

- `deploy/README.md`
- `deploy/docs/ansible-deployment-plan.md`
- `AGENTS.md`
- `plan_dev/MASTER_PLAN.md`
- `plan_dev/STATUS.md`
- `plan_dev/daily/2026-08-04.md`

### Validación automática obligatoria

- `git diff --check`
- verificación de links/rutas locales mencionadas en el plan
- comprobar que no se agregaron archivos Ansible/Vagrant ejecutables ni cambios runtime

### Validación manual sugerida

- revisar la tabla de decisiones pendientes con infraestructura, seguridad, DBA y responsables funcionales;
- aprobar host + Apache/PHP-FPM o indicar si existe plataforma institucional de contenedores;
- confirmar dominio, topología, acceso SSH, TLS, secretos, storage y backups.

### Stop conditions

- la planificación puede cerrarse con decisiones marcadas pendientes;
- no comenzar implementación hasta promover una etapa nueva y resolver las decisiones bloqueantes indicadas en el documento.

### Resultado

`done`: planificación documental completa; implementación expresamente pendiente.

## M2 — Resolución inicial de decisiones de despliegue

### Estado

`done`

### Alcance ejecutado

- topología inicial de dos servidores, conservando modo single-host mediante pertenencia a ambos grupos;
- Debian 13, PHP 8.4 y PostgreSQL 17 como stack recomendado con paquetes nativos;
- dominio/TLS parametrizados y SSH directo con bastion opcional;
- Ansible Vault con password file fuera del repositorio;
- explicación y estrategia inicial de storage privado;
- ruta local, retención y objetivos provisionales de backup;
- origen de releases mediante GitHub y tag/SHA;
- política inicial de logs sensibles y monitoreo.

### Validación automática obligatoria

- `git diff --check`
- validación de rutas locales citadas
- comprobar que no se agregaron secretos ni artefactos Ansible ejecutables

### Validación manual sugerida

- confirmar con negocio RPO 24 h, RTO 4 h y retención 7/4/6;
- informar IP/hostname, usuario y clave SSH cuando se pase de Vagrant a servidores reales;
- definir dominio, DNS, autoridad TLS, destino externo de backups y canal de alertas antes de sus etapas.

### Resultado

`done`: decisiones suficientes para promover posteriormente `D1`; siguen pendientes valores de infraestructura real que no bloquean la estructura base.

## M3 — Arquitectura desacoplada por tecnología y versión

### Estado

`done`

### Alcance ejecutado

- regla estable para aislar documentación, código y tests por tecnología/versión;
- propuesta intermedia de perfiles declarativos, reemplazada luego por M4;
- inventory inicialmente pensado por perfil, corregido luego por M4;
- playbooks generales sin lógica de paquetes, servicios o paths específicos;
- separación de Apache y PHP en componentes independientes;
- combinación inicial documentada, desacoplada luego en variables independientes por M4;
- preservación de implementaciones validadas, refinada por M4 dentro de cada tecnología;
- ajuste de etapas para implementar y probar cada componente por separado.

### Validación automática obligatoria

- `git diff --check`
- comprobar consistencia entre estructura, contrato, inventories y etapas
- confirmar que no se crearon todavía roles, perfiles YAML ni otros artefactos ejecutables

### Validación manual sugerida

- aprobar el principio de componentes versionados e inmutabilidad por compatibilidad;
- durante D1 validar con un spike que la resolución de roles de Ansible mantenga esta estructura sin paths frágiles.

### Resultado

`done`: planificación desacoplada; ninguna versión queda como condición global del deploy.

> Nota: M4 refina y reemplaza el mecanismo de perfiles monolíticos propuesto aquí. Se conserva M3 como trazabilidad histórica del razonamiento.

## M4 — Selección independiente de versiones desde inventory

### Estado

`done`

### Alcance ejecutado

- revisión del patrón de `elecciones/msa/deploy/provisioning`, especialmente `pg_version` y sus hardcodes residuales;
- eliminación del perfil combinado Debian/Apache/PHP/PostgreSQL;
- variables independientes para sistema operativo, web server, PHP y PostgreSQL;
- carpeta estable por tecnología/proveedor con archivos de soporte por versión;
- cambio rápido de una versión ya soportada modificando solo inventory;
- extensión de una versión nueva limitada a la tecnología afectada;
- compatibilidad cruzada únicamente donde exista dependencia técnica real.

### Validación automática obligatoria

- `git diff --check`
- búsqueda de referencias obsoletas al perfil monolítico
- consistencia entre estructura, inventory conceptual, etapas y estado
- confirmar que no se agregaron artefactos ejecutables

### Validación manual sugerida

- aprobar variables independientes como contrato del inventory;
- en D1 probar `include_vars`/`include_tasks` seguro con allowlist de versiones.

### Resultado

`done`: PostgreSQL, PHP, Apache y sistema operativo pueden evolucionar independientemente cuando la versión esté soportada.

## M5 — Simplificación y alineación con provisioning de elecciones

### Estado

`done`

### Alcance ejecutado

- adopción de `deploy/provisioning/` como raíz familiar de Ansible;
- playbooks visibles `site.yml`, `app.yml`, `database.yml` y operativos;
- `group_vars`, inventories, roles estándar, `Vagrantfile` y futuro `bin/deploy` corto;
- roles simples `common`, `postgresql`, `php`, `apache`, `laravel`, `scheduler`, `firewall`, `backup`, `tls` y `monitoring`;
- versiones independientes mediante `roles/<tecnologia>/vars/versions/`;
- alcance ordenado de la primera entrega funcional;
- prácticas útiles tomadas de elecciones y exclusión explícita de hardcodes, passwords y complejidad histórica.

### Validación automática obligatoria

- `git diff --check`
- consistencia entre árbol, etapas, roles y variables de inventory
- búsqueda de restos de `technologies/`/`orchestration/`
- confirmar que el cambio sigue siendo solo documental

### Validación manual sugerida

- confirmar que la estructura `deploy/provisioning/` resulta familiar y suficientemente simple;
- aprobar Apache/PHP-FPM sobre host para promover D1.

### Resultado

`done`: primera versión planificada con estructura estándar, familiar y sin abstracciones innecesarias.
