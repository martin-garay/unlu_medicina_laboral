# Daily Plan
## Fecha
2026-04-26

## Objetivo del día
Retomar el bloque `M5` antes de avanzar a logs/admin/Ansible y convertir el diseño de anticipo/certificado en una secuencia ejecutable que cubra los requerimientos RF-03, RF-03.1 y RF-03.2.

Este daily reemplaza como fuente operativa vigente al tramo pendiente del daily 2026-04-24. El daily 2026-04-24 queda como histórico de decisiones y ejecución parcial.

---

## Decisiones confirmadas para este daily

- `certificado` se refiere a la entidad actual `AnticipoCertificado`; no se crea una entidad principal nueva `certificados`.
- El estado inicial de avisos y anticipos/certificados será `inicial` con etiqueta funcional "Inicial".
- No se debe avanzar a `M6 - Relevar logs actuales y definir estructura objetivo` hasta cerrar o bloquear explícitamente esta cadena `M5.x`.
- RF-03 se considera cubierto solo cuando el anticipo/certificado se materializa al confirmar, genera número único, queda en estado `inicial` y conserva trazabilidad.
- RF-03.1 se considera cubierto solo cuando el flujo permita adjuntar hasta `medicina_laboral.certificados.max_files` archivos/imágenes antes de confirmar.
- RF-03.2 se considera cubierto solo cuando exista relación N a N entre avisos y anticipos/certificados mediante pivot, manteniendo compatibilidad temporal con `anticipos_certificado.aviso_id`.

---

## Reglas de ejecución del día

- Leer antes de empezar:
  - `AGENTS.md`
  - `plan_dev/MASTER_PLAN.md`
  - `plan_dev/STATUS.md`
  - este archivo
- Ejecutar los milestones `M5.x` en orden.
- No avanzar a `M6` hasta que todos los `M5.x` estén `done`, `blocked` o `needs_review`.
- Si falla una validación obligatoria, no avanzar al siguiente milestone.
- Actualizar `plan_dev/STATUS.md` al cerrar cada milestone o al quedar bloqueado.
- Si se toca runtime, correr `make test`.
- Si se modifican relaciones, tablas o flujo conversacional, actualizar `docs/` y `docs/diagrams/` afectados.
- Todo milestone `done` con cambios listos debe cerrarse con commit chico y trazable, salvo instrucción explícita de no commitear.

---

## M5.0
### ID
M5.0

### Nombre
Reabrir M5 y fijar plan ejecutable RF-03

### Objetivo
Crear el daily vigente del 2026-04-26, declarar que `inicial` es el estado funcional elegido y ordenar los cortes de implementación antes de avanzar a M6.

### Alcance
- crear `plan_dev/daily/2026-04-26.md`
- registrar que el tramo M5 queda reabierto como cadena `M5.x`
- actualizar `plan_dev/STATUS.md`
- definir milestones de implementación para:
  - estado `inicial`
  - carga de hasta 3 adjuntos
  - relación N a N entre avisos y anticipos/certificados
  - sincronización documental y diagramas
- correr validación automática base

### No incluye
- migraciones runtime
- cambios en modelos Eloquent
- cambios en handlers del flujo
- cambios de storage real/binario

### Dependencias
- daily 2026-04-24 con M5 documental como antecedente
- decisión humana de usar estado `inicial`

### Validación automática obligatoria
- `make test`
- `git diff --check`

### Validación manual sugerida
- revisar que el orden `M5.1 -> M5.2 -> M5.3 -> M5.4` cubra RF-03, RF-03.1 y RF-03.2 antes de M6

### Stop conditions específicas
- si no puede ejecutarse la suite base, dejarlo explícito en `STATUS.md`

### Entregable esperado
- daily vigente creado
- `STATUS.md` apuntando a `M5.1` como próximo milestone
- baseline de tests documentado

### Si queda bloqueado, ¿se puede avanzar al siguiente milestone?
- `no`

### Estado
`done`

### Resultado de ejecución
- Se creó el daily vigente `plan_dev/daily/2026-04-26.md`.
- Se reabrió el tramo M5 como cadena `M5.x` antes de avanzar a M6.
- Se confirmó `inicial` como estado objetivo para avisos y anticipos/certificados.
- Se incorporaron milestones específicos para RF-03, RF-03.1 y RF-03.2.
- Se ejecutó baseline automática: `make test` pasó con `115 passed`, `384 assertions`.
- `git diff --check` no reportó errores de whitespace.

---

## M5.1
### ID
M5.1

### Nombre
Implementar estado inicial en avisos y anticipos

### Objetivo
Alinear código, migraciones y tests con la decisión funcional de usar `inicial` como estado inicial de avisos y anticipos/certificados.

### Alcance
- agregar `avisos.estado` con default `inicial`
- cambiar default de `anticipos_certificado.estado` de `registrado` a `inicial`
- hacer que `AvisoService` persista explícitamente `estado = inicial`
- hacer que `AnticipoCertificadoService` persista explícitamente `estado = inicial`
- actualizar modelos/fillable/casts si corresponde
- actualizar `tests/Concerns/CreatesTestingSchema.php`
- ajustar tests existentes que esperan `registrado`
- agregar o ajustar tests de servicio para ambos estados iniciales
- actualizar documentación de modelo/validaciones si aparece una diferencia respecto de lo ya diseñado

### No incluye
- workflow administrativo de cambios de estado
- historial de estados
- reglas finales de negocio para `observado`

### Dependencias
- M5.0 `done`

### Validación automática obligatoria
- `make test`

### Validación manual sugerida
- crear un aviso y un anticipo desde el flujo local y confirmar que ambos quedan en `inicial`

### Stop conditions específicas
- si la migración de cambio de default no puede escribirse de forma portable para PostgreSQL y SQLite de tests, dejar `blocked`

### Entregable esperado
- avisos y anticipos/certificados creados con estado `inicial`
- tests actualizados

### Si queda bloqueado, ¿se puede avanzar al siguiente milestone?
- `no`

### Estado
`pending`

---

## M5.2
### ID
M5.2

### Nombre
Permitir hasta 3 adjuntos antes de confirmar certificado

### Objetivo
Resolver RF-03.1 asegurando que el flujo conversacional permita cargar más de un archivo o imagen hasta el máximo configurado antes de pasar a confirmación final.

### Alcance
- relevar el paso actual `CertificadoAdjuntoStepHandler`
- definir e implementar una transición explícita para:
  - agregar otro adjunto
  - confirmar con los adjuntos ya cargados
  - bloquear el cuarto adjunto
- conservar `medicina_laboral.certificados.max_files` como fuente de verdad
- mantener validación de MIME y tipo de mensaje desde config
- actualizar textos en `lang/es/*.php`
- actualizar templates Blade si el resumen necesita mostrar lista de adjuntos
- ajustar o agregar handlers si conviene separar captura de adjunto y decisión posterior
- agregar tests unitarios de handlers:
  - primer adjunto no confirma automáticamente si todavía puede agregar más
  - segundo/tercer adjunto se acumulan
  - cuarto adjunto se rechaza
  - confirmación final muestra cantidad y nombres/referencias correctas
- agregar feature test si el contrato de `ConversationInteractionService` cambia

### No incluye
- descarga real del binario desde WhatsApp
- cambio de storage a S3
- administración de adjuntos desde backoffice

### Dependencias
- M5.1 `done`

### Validación automática obligatoria
- `make test`

### Validación manual sugerida
- recorrer flujo de anticipo en canal interno o WhatsApp con 1, 2 y 3 adjuntos
- verificar que no se permita un cuarto adjunto

### Stop conditions específicas
- si el contrato actual de salida no permite representar la decisión "agregar otro o confirmar" sin cambiar varias capas, dejar `needs_review` con propuesta técnica

### Entregable esperado
- RF-03.1 cubierto para hasta 3 adjuntos configurables
- tests de acumulación y límite de adjuntos

### Si queda bloqueado, ¿se puede avanzar al siguiente milestone?
- `no`

### Estado
`pending`

---

## M5.3
### ID
M5.3

### Nombre
Implementar relación N a N aviso-certificado

### Objetivo
Resolver RF-03.2 agregando una relación muchos a muchos entre avisos y anticipos/certificados, sin romper compatibilidad con `anticipos_certificado.aviso_id`.

### Alcance
- crear migración para tabla pivot `anticipo_certificado_aviso`
- incluir campos:
  - `id`
  - `anticipo_certificado_id`
  - `aviso_id`
  - `origen`
  - `estado_vinculo`
  - `metadata`
  - timestamps
- agregar unique compuesto `anticipo_certificado_id` + `aviso_id`
- agregar índices y foreign keys
- backfill desde `anticipos_certificado.aviso_id`
- agregar `AnticipoCertificado::avisos()` como `belongsToMany`
- migrar o complementar `Aviso::anticiposCertificado()` para leer por pivot sin eliminar la relación legacy hasta decidir deprecación
- mantener `AnticipoCertificado::aviso()` como relación legacy
- hacer que `AnticipoCertificadoService::createFromConversation()` escriba:
  - `anticipos_certificado.aviso_id`
  - fila pivot con `origen = conversacion`
- actualizar `tests/Concerns/CreatesTestingSchema.php`
- agregar tests de:
  - migración/backfill
  - creación de pivot desde servicio
  - relación anticipo -> avisos
  - relación aviso -> anticipos/certificados
  - unique compuesto para evitar duplicados

### No incluye
- UI/admin para vincular avisos adicionales
- reglas automáticas de asociación múltiple
- eliminación de `anticipos_certificado.aviso_id`

### Dependencias
- M5.2 `done`

### Validación automática obligatoria
- `make test`

### Validación manual sugerida
- crear anticipo desde flujo y verificar que el aviso inicial quede tanto en `aviso_id` como en pivot
- crear manualmente un segundo vínculo y verificar navegación por relaciones Eloquent

### Stop conditions específicas
- si aparece una incompatibilidad entre la estrategia de pivot y los tests de SQLite/PostgreSQL, dejar `blocked` con detalle

### Entregable esperado
- RF-03.2 cubierto a nivel modelo de datos, Eloquent y servicio de materialización
- compatibilidad temporal con `aviso_id`

### Si queda bloqueado, ¿se puede avanzar al siguiente milestone?
- `no`

### Estado
`pending`

---

## M5.4
### ID
M5.4

### Nombre
Sincronizar documentación y diagramas de anticipo/certificado

### Objetivo
Cerrar la cadena M5.x dejando documentación viva, diagramas y estado operativo alineados con la implementación de RF-03, RF-03.1 y RF-03.2.

### Alcance
- actualizar `docs/04-modelo-de-datos.md`
- actualizar `docs/07-flujo-anticipo-certificado.md`
- actualizar `docs/08-validaciones-y-reglas.md` si cambian reglas o errores
- actualizar `docs/12-decisiones-tecnicas.md`
- actualizar `docs/diagrams/db/medicina-laboral.dbml`
- actualizar `docs/diagrams/classes/conversation-engine.puml` si cambian relaciones relevantes
- actualizar `docs/diagrams/flows/anticipo-certificado.mmd` si cambia la secuencia de adjuntos
- regenerar diagramas si las herramientas están disponibles
- actualizar `plan_dev/STATUS.md`

### No incluye
- nuevos cambios runtime fuera de ajustes documentales derivados
- admin de certificados
- logs M6

### Dependencias
- M5.3 `done`

### Validación automática obligatoria
- `make test`
- `make diagrams-check` si está disponible y el entorno lo permite
- `git diff --check`

### Validación manual sugerida
- revisar que RF-03, RF-03.1 y RF-03.2 puedan mapearse desde docs a código y tests

### Stop conditions específicas
- si el renderizado de diagramas falla por herramienta faltante pero los fuentes están actualizados, documentar el límite en `STATUS.md`

### Entregable esperado
- documentación y diagramas alineados
- `STATUS.md` indicando próximo milestone recomendado `M6`

### Si queda bloqueado, ¿se puede avanzar al siguiente milestone?
- `no`

### Estado
`pending`

---

## M6
### ID
M6

### Nombre
Relevar logs actuales y definir estructura objetivo

### Objetivo
Retomar el M6 del daily 2026-04-24 recién después de cerrar la cadena M5.x.

### Estado
`pending`

### Nota
No ejecutar hasta que `M5.1`, `M5.2`, `M5.3` y `M5.4` estén cerrados o bloqueados explícitamente.

---

## Criterio de cierre del día

- `plan_dev/STATUS.md` debe indicar el último milestone ejecutado y su resultado.
- No debe quedar como próximo paso M6 mientras haya un `M5.x` pendiente.
- Si se implementa runtime, debe correrse `make test`.
- Si cambian relaciones o flujo, deben actualizarse docs y diagramas.

## Decisiones humanas esperadas al cierre del día

- confirmar si `aviso_id` quedará como cache del primer aviso o se eliminará luego de migrar lecturas a pivot
- confirmar si las asociaciones adicionales de avisos serán manuales, automáticas o mixtas
- confirmar si avisos `observado` deben poder recibir anticipo o si pasan a ser bloqueantes
