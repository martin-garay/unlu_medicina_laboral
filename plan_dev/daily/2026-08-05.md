# Daily 2026-08-05 — Preparación para iniciar provisioning

## Objetivo del día

Cerrar las decisiones mínimas del entorno Vagrant y preparar el arranque de `D1` sin implementar todavía roles ni playbooks.

## M1 — URLs, TLS, SSH, Vault, backups y storage para Vagrant

### Estado

`done`

### Alcance ejecutado

- separación entre URL local, URL pública temporal de ngrok y dominio productivo futuro;
- HTTPS de Vagrant con Apache y CA local;
- bootstrap SSH con usuario `vagrant` y creación posterior del usuario `deploy`;
- bastion explicado y deshabilitado para el alcance actual;
- reutilización de credenciales actuales de Meta mediante Vault cifrado;
- password file de Ansible Vault creado fuera del repositorio con permisos `0600`;
- storage privado local recomendado para MVP;
- backup externo recomendado con restic/S3-compatible y simulación mediante carpeta persistente del host en Vagrant.

### Archivos modificados

- `deploy/docs/ansible-deployment-plan.md`
- `plan_dev/daily/2026-08-05.md`
- `plan_dev/STATUS.md`

### Validación automática obligatoria

- verificar existencia y permisos del password file sin leer su contenido;
- `git diff --check`;
- validar rutas locales citadas;
- confirmar que no se versionaron secretos ni artefactos ejecutables.

### Validación manual sugerida

- al iniciar D1, decidir si el hostname local default `medicina-laboral.test` resulta conveniente;
- confirmar disponibilidad futura de un proveedor S3-compatible institucional o elegir SFTP.

### Resultado

`done`: decisiones suficientes para crear un daily de implementación D1.

## Próximo milestone

Crear un daily específico para `D1 - estructura mínima de provisioning` y trabajar únicamente su primer milestone pendiente.
