# Daily 2026-09-04 — Transferencia de vendor sin rsync

## Objetivo del día

Continuar `M4 - Deploy completo sin security/firewall` reemplazando la
transferencia bloqueada de `vendor/` por un artefacto comprimido compatible con
el salto SSH institucional.

## M4 — Deploy completo sin security/firewall

### Estado

`needs_review`

### Contexto

- Composer local instala las dependencias usando el proxy de PC Uni.
- El post-script de Laravel/Filament dispone de sus directorios escribibles.
- `ansible.posix.synchronize` quedó esperando más de 30 minutos y no creó
  `vendor/` en testing.
- La cancelación dejó sin procesos rsync residuales, sin migraciones y sin
  activar el release incompleto.

### Cambio

- Generar `testing-2026-08-24-01-vendor.tar.gz` desde el checkout exacto.
- Copiarlo al destino con `ansible.builtin.copy` sobre SFTP.
- Extraerlo dentro del release con owner `deploy` y group `www-data`.
- Eliminar el temporal remoto.
- Mantener la validación de plataforma, migraciones y activación posteriores.
- Reservar rsync solamente para `application_transfer_strategy: rsync`.

### Validación automática obligatoria

```bash
cd deploy/provisioning
bin/check-deploy
ansible-playbook -i inventories/testing/hosts.yml site.yml --check --diff
ansible-playbook -i inventories/testing/hosts.yml site.yml
ansible-playbook -i inventories/testing/hosts.yml playbooks/monitoring.yml
ansible-playbook -i inventories/testing/hosts.yml site.yml
```

La segunda ejecución real debe demostrar idempotencia sin cambios
injustificados.

### Validación manual sugerida

- Confirmar que `current` apunte a `testing-2026-08-24-01`.
- Verificar `/up`, backoffice, scheduler y logs.
- Confirmar que no quede el artefacto temporal de vendor en `/tmp`.

### Resultado de esta ejecución

- `git diff --check`: OK.
- `bin/yamllint` sobre defaults, tareas e inventory: OK.
- `bin/ansible-playbook ... --syntax-check`: OK.
- `bin/check-deploy`: OK, 48 archivos sin fallas ni warnings de lint.
- Aplicación remota: pendiente desde PC Uni; M4 permanece en `needs_review`.
- El apply posterior transfirió `vendor/` (102 MB) y llegó a activar el release,
  pero el módulo `uri` falló por incompatibilidad entre su conexión HTTPS y
  `urllib3` remoto. El rollback aceptó incorrectamente `current` como destino y
  creó un enlace circular.
- Se recuperó manualmente `current -> releases/testing-2026-08-24-01` y `/up`
  respondió HTTP 200 desde testing.
- El rol valida ahora `/up` con `curl`, sólo considera recuperables directorios
  existentes bajo `releases/` y usa `follow: false` al activar o restaurar.
- La corrección pasó `git diff --check`, `yamllint`, `syntax-check` y
  `bin/check-deploy`; falta el apply remoto, monitoreo e idempotencia.

### Stop

- Detener si Composer local falla.
- Detener si el archivo se extrae fuera del release seleccionado.
- Detener si falla `composer check-platform-reqs`.
- Detener si se ejecutan hardening, firewall o nftables.
