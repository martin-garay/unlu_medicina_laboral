# Daily Plan
## Fecha
2026-04-28

## Objetivo del dia
Preparar y ejecutar el siguiente bloque del backoffice despues de `I1`: resolver `BO-001` y avanzar con `I2 - Auth, usuarios, roles y permisos`.

El foco del dia es dejar una base de permisos administrativa mantenible antes de crear Resources funcionales de avisos, certificados, conversaciones o reportes.

---

## Estado actual resumido

- `I1 - Base tecnica de Filament`: `done`.
- Filament `v5.6.1` instalado.
- Panel base disponible en `/admin`.
- Login administrativo disponible en `/admin/login`.
- `App\Models\User` existe y restringe acceso con `is_admin`.
- Tabla `users` existe.
- Cache local corregida con `CACHE_STORE=file`.
- No existe todavia stack de roles/permisos.
- No existen todavia Resources funcionales de Filament.
- `BO-001` sigue pendiente y bloquea `I2`.
- `BO-002` sigue pendiente y bloquea `I4`.
- `BO-003` sigue pendiente y bloquea `I7`.

---

## Decisiones propuestas para ejecutar hoy

Estas decisiones quedan como propuesta operativa del daily. Si se detecta una contradiccion funcional o tecnica relevante, frenar en `needs_review`.

### Stack de permisos
- Usar Spatie Laravel Permission como stack base.
- Mantener Gates/Policies de Laravel para integracion con Filament cuando corresponda.
- No implementar permisos por campo o columna.
- No inventar un sistema propio de permisos salvo bloqueo tecnico fuerte con Spatie.

### Roles iniciales
- `admin`: administracion completa del backoffice.
- `auditor`: lectura de datos operativos y auditoria, sin acciones de estado ni administracion de usuarios.
- `director`: lectura operativa y reportes, sin administracion de usuarios, auditoria sensible ni acciones de estado.

### Permisos base iniciales
- `backoffice.access`
- `dashboard.view`
- `users.view`
- `users.manage`
- `roles.view`
- `roles.manage`
- `avisos.view`
- `certificados.view`
- `conversaciones.view`
- `auditoria.view`
- `reportes.view`

Los permisos de descarga/visualizacion de archivos medicos quedan para `I4`, junto con storage privado y auditoria de lectura.

---

## Reglas de ejecucion de este daily

- Ejecutar los milestones en orden.
- El primer milestone pendiente despues de crear este plan es `D1`.
- No avanzar a Resources de avisos/certificados antes de cerrar `I2`.
- No implementar storage privado en este daily salvo que se cierre I2 y se abra explicitamente un nuevo daily/milestone.
- Si la instalacion de dependencias requiere red y falla por entorno, reintentar con permisos adecuados; si sigue fallando, dejar `blocked`.
- Si una validacion obligatoria falla, corregir una vez de forma acotada; si persiste, dejar `blocked`.
- Todo milestone cerrado con cambios debe quedar en commit chico y trazable.
- No modificar `plan_dev/daily/2026-04-26.md`.

---

## D0
### ID
D0

### Nombre
Abrir daily ejecutable de backoffice para I2

### Objetivo
Crear el plan diario 2026-04-28 con estado actual, decisiones propuestas, milestones ejecutables y primer pendiente claro.

### Alcance
- relevar `STATUS.md`, `BACKLOG.md` y `docs/backoffice/implementation-plan.md`
- crear `plan_dev/daily/2026-04-28.md`
- actualizar `plan_dev/STATUS.md`
- conservar el cambio de README con usuario local de prueba como documentacion operativa

### No incluye
- instalar Spatie Laravel Permission
- crear roles o permisos en base
- crear Resources de Filament
- crear migrations runtime nuevas

### Validacion automatica obligatoria
- `git diff --check`

### Validacion manual sugerida
- confirmar que `D1` sea el primer milestone pendiente
- confirmar que la matriz propuesta alcanza para arrancar I2

### Stop conditions especificas
- si existe otro daily activo mas reciente que contradiga este plan, dejar `blocked`

### Estado
`done`

### Resultado de ejecucion
- Daily creado para ejecutar `BO-001` e `I2`.
- Se dejo `D1` como primer milestone pendiente.

---

## D1
### ID
D1

### Nombre
Resolver BO-001: stack y matriz inicial de permisos

### Objetivo
Cerrar la decision funcional y tecnica minima de permisos para poder implementar I2 sin improvisar.

### Alcance
- crear o actualizar documentacion de matriz de permisos del backoffice
- confirmar Spatie Laravel Permission como stack elegido
- documentar roles `admin`, `auditor`, `director`
- documentar permisos base iniciales
- actualizar `docs/backoffice/security-and-audit.md`
- actualizar `docs/backoffice/implementation-plan.md` si corresponde
- actualizar `plan_dev/BACKLOG.md` marcando `BO-001` como promovido/resuelto o con referencia al daily
- actualizar `plan_dev/STATUS.md`

### No incluye
- instalar dependencias
- crear migrations
- seeders
- cambios runtime

### Dependencias
- `D0` done

### Validacion automatica obligatoria
- `git diff --check`

### Validacion manual sugerida
- revisar que `auditor` no tenga permisos de escritura
- revisar que `director` no tenga permisos de auditoria sensible ni administracion de usuarios
- revisar que permisos de archivos medicos queden diferidos a I4

### Stop conditions especificas
- si no se acepta Spatie como stack base, dejar `needs_review` y no avanzar a D2
- si falta definicion de permisos minimos para `auditor` o `director`, dejar `needs_review`

### Entregable esperado
- `BO-001` documentado y desbloqueando I2

### Commit sugerido
- `docs: definir matriz inicial de permisos del backoffice`

### Estado
`done`

### Resultado de ejecucion
- Se confirmo Spatie Laravel Permission como stack base de permisos con guard `web`.
- Se creo `docs/backoffice/permissions.md` con roles `admin`, `auditor`, `director` y matriz inicial.
- Se actualizaron `docs/backoffice/security-and-audit.md`, `docs/backoffice/implementation-plan.md`, `docs/backoffice/README.md` y `plan_dev/BACKLOG.md`.
- `BO-001` quedo marcado como resuelto.
- No se instalaron dependencias ni se tocaron archivos runtime.

### Validacion ejecutada
- `git diff --check`

---

## D2
### ID
D2

### Nombre
Instalar y configurar stack de permisos

### Objetivo
Instalar Spatie Laravel Permission y dejarlo integrado con el modelo `User`.

### Alcance
- agregar dependencia `spatie/laravel-permission`
- publicar config y migrations necesarias
- agregar trait `HasRoles` a `App\Models\User`
- configurar guard `web`
- correr migraciones
- agregar tests de modelo/permiso basicos
- actualizar documentacion operativa si corresponde
- actualizar `plan_dev/STATUS.md`

### No incluye
- Resources de Filament para administrar usuarios/roles
- permisos de certificados medicos sensibles
- auditoria administrativa

### Dependencias
- `D1` done

### Validacion automatica obligatoria
- `make migrate`
- `make test`
- `git diff --check`

### Validacion manual sugerida
- verificar que un usuario sin rol no acceda al panel
- verificar que un usuario con rol `admin` pueda acceder al panel

### Stop conditions especificas
- si Composer no puede instalar la dependencia, dejar `blocked`
- si migrations de Spatie colisionan con tablas existentes, dejar `blocked`
- si el acceso al panel queda abierto por error, no avanzar a D3

### Entregable esperado
- Spatie instalado e integrado con `User`

### Commit sugerido
- `feat: agregar stack de permisos del backoffice`

### Estado
`done`

### Resultado de ejecucion
- Se instalo `spatie/laravel-permission` version `6.25.0`.
- Se publico `config/permission.php`.
- Se publico y ejecuto la migration `2026_04_28_173621_create_permission_tables.php`.
- Se agrego `Spatie\Permission\Traits\HasRoles` a `App\Models\User`.
- Se agrego `tests/Feature/Backoffice/PermissionStackTest.php` para validar rol, permiso y guard `web`.

### Validacion ejecutada
- `make artisan CMD='config:clear'`
- `make migrate`
- `make test`: `131 passed`, `433 assertions`
- `git diff --check`

---

## D3
### ID
D3

### Nombre
Crear roles, permisos base y seed local

### Objetivo
Materializar la matriz definida en D1 en datos iniciales reproducibles.

### Alcance
- crear enum/configuracion de permisos base si corresponde
- crear seeder de roles y permisos
- crear o ajustar seeder de usuario admin local de desarrollo si corresponde
- asegurar idempotencia de seeders
- agregar tests de roles/permisos
- actualizar README si cambia el comando recomendado de usuario admin
- actualizar `plan_dev/STATUS.md`

### No incluye
- UI de gestion de usuarios
- UI de gestion de roles
- Resources de negocio

### Dependencias
- `D2` done

### Validacion automatica obligatoria
- `make artisan CMD="db:seed --class=<SeederCorrespondiente>"`
- `make test`
- `git diff --check`

### Validacion manual sugerida
- crear usuario admin local y confirmar login
- confirmar que `admin@admin.com` pueda acceder si tiene rol/permiso correcto

### Stop conditions especificas
- si los seeders no son idempotentes, no avanzar
- si el README queda desalineado con la forma real de crear admin local, corregir antes de cerrar

### Entregable esperado
- roles y permisos base reproducibles

### Commit sugerido
- `feat: agregar roles y permisos base de backoffice`

### Estado
`pending`

---

## D4
### ID
D4

### Nombre
Restringir acceso del panel por permiso

### Objetivo
Reemplazar la restriccion transitoria `is_admin` por acceso basado en rol/permiso.

### Alcance
- actualizar `User::canAccessPanel()`
- conservar compatibilidad local solo si queda documentada
- agregar tests HTTP para:
  - invitado redirige a login
  - usuario sin permiso no accede
  - usuario con `backoffice.access` accede
- actualizar documentacion y status

### No incluye
- Resources de usuarios/roles
- filtros de visibilidad por modulo

### Dependencias
- `D3` done

### Validacion automatica obligatoria
- `make test`
- `git diff --check`

### Validacion manual sugerida
- login local con usuario admin y apertura de `/admin`

### Stop conditions especificas
- si `admin` no puede acceder al panel
- si un usuario sin permisos accede al panel

### Entregable esperado
- panel protegido por permisos reales

### Commit sugerido
- `feat: restringir panel por permiso de backoffice`

### Estado
`pending`

---

## D5
### ID
D5

### Nombre
Cierre operativo de I2 base

### Objetivo
Cerrar el bloque ejecutado, consolidar estado y decidir si I2 continua con UI de usuarios/roles o se corta antes del siguiente daily.

### Alcance
- actualizar `docs/backoffice/implementation-plan.md`
- actualizar `plan_dev/STATUS.md`
- actualizar `plan_dev/BACKLOG.md` si aparecieron pendientes nuevos
- dejar proximo paso recomendado
- ejecutar validaciones finales

### No incluye
- iniciar `I3 - Auditoria administrativa base`
- iniciar `I4 - Storage privado de certificados`
- crear Resources de avisos/certificados

### Dependencias
- `D4` done o milestone previo en estado `blocked`/`needs_review`

### Validacion automatica obligatoria
- `make test`
- `git diff --check`

### Validacion manual sugerida
- revisar historial de commits del dia
- probar login administrativo en navegador

### Stop conditions especificas
- si queda una decision humana pendiente para cerrar I2 base, marcar `needs_review`

### Entregable esperado
- I2 base cerrado o estado claro de bloqueo

### Commit sugerido
- `docs: cerrar ejecucion de permisos de backoffice`

### Estado
`pending`

---

## Criterio de cierre del dia

El dia se considera exitoso si:

- `BO-001` queda resuelto documentalmente.
- Spatie Laravel Permission queda instalado e integrado, o el bloqueo queda claramente trazado.
- El panel queda protegido por permisos reales, no solo por `is_admin`, si D2-D4 se completan.
- `make test` queda verde para cualquier cambio runtime.
- `STATUS.md` refleja la ultima ejecucion.
